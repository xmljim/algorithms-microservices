eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
spring:
  application:
    name: retirementservice
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configserver