spring:
  application:
    name: statisticsservice
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configserver

algorithms:
  statistics:
    provider:
      version: 1.0.0
      name: Statistics