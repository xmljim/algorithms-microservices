spring:
  application:
    name: stockservice
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configserver
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}