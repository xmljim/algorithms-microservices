spring:
  application:
    name: stock-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}